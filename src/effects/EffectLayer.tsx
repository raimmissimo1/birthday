import type { GiftEffectsConfig } from "../giftConfig"; export function EffectLayer({ effects, finale }: { effects: GiftEffectsConfig; finale: boolean }) { const items = finale ? effects.finale : effects.ambient; return <div className={`effect-layer effect-${effects.intensity}`} aria-hidden="true">{items.map((item) => <span key={item} className={`effect effect-${item}`} />)}</div>; }
