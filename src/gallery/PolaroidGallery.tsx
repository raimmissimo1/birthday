import type { GalleryPhoto } from "./galleryTypes"; export function PolaroidGallery({ photos }: { photos: GalleryPhoto[] }) { return <div className="polaroid-gallery">{photos.map((photo, index) => <figure key={photo.id} tabIndex={0}><img loading="lazy" src={photo.image} alt={photo.alt ?? "Памятный момент"} /><figcaption>{photo.caption ?? `Воспоминание ${index + 1}`}</figcaption></figure>)}</div>; }
