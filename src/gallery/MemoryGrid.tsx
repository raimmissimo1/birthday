import { useEffect, useState } from "react"; import type { GalleryPhoto } from "./galleryTypes"; export function MemoryGrid({ photos }: { photos: GalleryPhoto[] }) { const [active, setActive] = useState<GalleryPhoto | null>(null); useEffect(() => { const key = (event: KeyboardEvent) => { if (event.key === "Escape") setActive(null); }; window.addEventListener("keydown", key); return () => window.removeEventListener("keydown", key); }, []); return <><div className="memory-grid">{photos.map((photo) => <button key={photo.id} type="button" onClick={() => setActive(photo)}><img loading="lazy" src={photo.image} alt={photo.alt ?? "Памятный момент"} /></button>)}</div>{active && <div className="gallery-lightbox" role="dialog" aria-modal="true" aria-label="Просмотр фотографии"><button type="button" onClick={() => setActive(null)} aria-label="Закрыть">Закрыть</button><img src={active.image} alt={active.alt ?? "Памятный момент"} /></div>}</>; }
