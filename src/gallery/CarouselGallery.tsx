import { useState } from "react"; import type { GalleryPhoto } from "./galleryTypes"; export function CarouselGallery({ photos }: { photos: GalleryPhoto[] }) { const [index, setIndex] = useState(0); const current = photos[index]; if (!current) return null; return <div className="gallery-carousel" onKeyDown={(event) => { if (event.key === "ArrowLeft") setIndex((value) => Math.max(0, value - 1)); if (event.key === "ArrowRight") setIndex((value) => Math.min(photos.length - 1, value + 1)); }} tabIndex={0}><img src={current.image} alt={current.alt ?? "Памятный момент"} /><div><button type="button" onClick={() => setIndex((value) => Math.max(0, value - 1))} aria-label="Предыдущее фото">Назад</button><span>{index + 1} / {photos.length}</span><button type="button" onClick={() => setIndex((value) => Math.min(photos.length - 1, value + 1))} aria-label="Следующее фото">Вперёд</button></div></div>; }
