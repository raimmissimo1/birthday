import type { CakePreset } from "./types"; import { Candle } from "./Candle"; export function CakeCandles({ preset, lit, y }: { preset: CakePreset; lit: boolean; y: number }) { const anchors = preset.candleAnchors ?? Array.from({ length: preset.candleCount }, (_, index) => { const angle = (index / preset.candleCount) * Math.PI * 2; return [Math.cos(angle) * .34, y, Math.sin(angle) * .34] as [number, number, number]; }); return <group>{anchors.slice(0, preset.candleCount).map((anchor, index) => <group key={index} position={anchor}><Candle lit={lit} color={preset.candleColor} flameColor={preset.flameColor} /></group>)}{lit && <pointLight position={[0, y + .42, 0]} color={preset.flameColor} intensity={preset.glow} distance={2.1} />}</group>; }
